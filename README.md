# PyTorch Chess Bot ♟️

A small chess engine written in **Python** using **PyTorch** and machine learning.

The bot uses a neural network to evaluate chess positions and choose moves. It is mainly built as a learning project to experiment with chess AI, PyTorch, and game-tree search.
